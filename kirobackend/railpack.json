{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "22",
    "python": "3.11"
  },
  "steps": {
    "build": {
      "commands": [
        "...",
        "python3 -m pip install --upgrade pip",
        "if [ \"$FORECAST_INSTALL_FULL_LSTM\" = \"true\" ]; then python3 -m pip install -r src/forecast/requirements.txt --target src/forecast/.python_packages; else python3 -m pip install -r src/forecast/requirements.railway.txt --target src/forecast/.python_packages; fi"
      ]
    }
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
